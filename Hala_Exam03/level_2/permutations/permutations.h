#ifndef PERMUTATIONS_H
#define PERMUTATIONS_H

#include <stdio.h>
#include <stdlib.h>

int ft_strlen(char *s);
int ft_strchr(char *s, char c);
void prem(char *argv, char *res, int pos, int len);

#endif